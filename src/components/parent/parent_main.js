import React, {  Component } from 'react'

export default class ParentMain extends Component {
    // logout = event => {
    //     event.preventDefault();
    //     localStorage.removeItem('token');
    //     localStorage.removeItem('user_role');
    //     window.location.reload();
    // }
    render() {
        return (
            <div>
                <h1>Родитель</h1>
            </div>
        )
    }
}
